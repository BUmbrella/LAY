<Window x:Class="LAY.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converters="clr-namespace:LAY.Converters"
        mc:Ignorable="d"
        Title="LAY 研磨测量"
        Icon="/Resource/LYA_icon_256.png"
        Height="900"
        Width="1600"
        Background="#0F172A"
        WindowStartupLocation="CenterScreen"
        AllowDrop="True"
        PreviewDragEnter="RootWindow_PreviewDragEnter"
        PreviewDragOver="RootWindow_PreviewDragOver"
        PreviewDrop="RootWindow_PreviewDrop">
    <Window.Resources>
        <SolidColorBrush x:Key="PanelBrush" Color="#111827" />
        <SolidColorBrush x:Key="PanelBorderBrush" Color="#22304A" />
        <SolidColorBrush x:Key="PrimaryBrush" Color="#38BDF8" />
        <SolidColorBrush x:Key="PrimaryDarkBrush" Color="#0284C7" />
        <SolidColorBrush x:Key="MutedTextBrush" Color="#94A3B8" />
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <converters:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter" />

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontSize" Value="14" />
        </Style>

        <Style TargetType="Button">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryDarkBrush}" />
            <Setter Property="Padding" Value="10,8" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="RootBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="10"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="RootBorder" Property="Opacity" Value="0.92" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="RootBorder" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.98" ScaleY="0.98" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="RootBorder" Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ListBox">
            <Setter Property="Background" Value="#0B1220" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="{StaticResource PanelBorderBrush}" />
        </Style>
    </Window.Resources>

    <Grid Background="#0F172A"
          AllowDrop="True"
          PreviewDragEnter="RootWindow_PreviewDragEnter"
          PreviewDragOver="RootWindow_PreviewDragOver"
          PreviewDrop="RootWindow_PreviewDrop">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="360" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0"
                AllowDrop="True"
                Margin="16"
                Background="{StaticResource PanelBrush}"
                BorderBrush="{StaticResource PanelBorderBrush}"
                BorderThickness="1"
                CornerRadius="18"
                Padding="18">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Border Height="96"
                        CornerRadius="24"
                        HorizontalAlignment="Stretch"
                        Margin="0,0,0,18">
                    <Border.Background>
                        <ImageBrush ImageSource="/Resource/APS.png"
                                    Stretch="UniformToFill"
                                    AlignmentX="Center"
                                    AlignmentY="Center" />
                    </Border.Background>
                </Border>

                <Button Grid.Row="1"
                        Content="{Binding StartStopText}"
                        Command="{Binding ToggleRunCommand}"
                        Height="48"
                        FontSize="16"
                        Margin="0,0,0,14" />

                <Button Grid.Row="2"
                        Content="查看输入"
                        Command="{Binding ShowInputCommand}"
                        Height="44"
                        Margin="0,0,0,14" />

                <Button Grid.Row="3"
                        Content="查看结果"
                        Command="{Binding ShowResultsCommand}"
                        Height="44"
                        Margin="0,0,0,14" />

                <Border Grid.Row="4"
                        Background="#0B1220"
                        BorderBrush="{StaticResource PanelBorderBrush}"
                        BorderThickness="1"
                        CornerRadius="14"
                        Padding="12">
                    <DockPanel LastChildFill="True">
                        <TextBlock DockPanel.Dock="Top"
                                   Text="日志"
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,10" />
                        <ListBox ItemsSource="{Binding Logs}"
                                 BorderThickness="0"
                                 Background="Transparent"
                                 ScrollViewer.VerticalScrollBarVisibility="Visible">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}"
                                               TextWrapping="Wrap"
                                               Foreground="{StaticResource MutedTextBrush}"
                                               FontSize="12"
                                               Margin="0,0,0,6" />
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </DockPanel>
                </Border>
            </Grid>
        </Border>

        <Border Grid.Column="1"
                AllowDrop="True"
                Margin="0,16,0,16"
                Background="{StaticResource PanelBrush}"
                BorderBrush="{StaticResource PanelBorderBrush}"
                BorderThickness="1"
                CornerRadius="18"
                Padding="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Orientation="Horizontal"
                                VerticalAlignment="Center">
                        <TextBlock Text="图像显示区"
                                   FontSize="18"
                                   FontWeight="SemiBold" />
                        <TextBlock Text="  /  鼠标滚轮缩放"
                                   Foreground="{StaticResource MutedTextBrush}"
                                   VerticalAlignment="Bottom" />
                    </StackPanel>

                    <StackPanel Grid.Column="1"
                                Orientation="Horizontal"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center">
                        <Button Content="缩小"
                                Command="{Binding ZoomOutCommand}"
                                Margin="0,0,8,0" />
                        <Button Content="还原"
                                Command="{Binding ResetZoomCommand}"
                                Margin="0,0,8,0" />
                        <Button Content="放大"
                                Command="{Binding ZoomInCommand}"
                                Margin="0,0,8,0" />
                        <TextBlock Text="{Binding ZoomFactor, StringFormat={}{0:P0}}"
                                   VerticalAlignment="Center"
                                   Foreground="{StaticResource MutedTextBrush}" />
                    </StackPanel>
                </Grid>

                <Border Grid.Row="1"
                        Background="#0B1220"
                        BorderBrush="#23314F"
                        BorderThickness="1"
                        CornerRadius="18"
                        Padding="20">
                    <ScrollViewer x:Name="PreviewScrollViewer"
                                  HorizontalScrollBarVisibility="Visible"
                                  VerticalScrollBarVisibility="Visible"
                                  CanContentScroll="False"
                                  Cursor="SizeAll"
                                  PreviewMouseWheel="PreviewScrollViewer_PreviewMouseWheel"
                                  PreviewMouseLeftButtonDown="PreviewScrollViewer_PreviewMouseLeftButtonDown"
                                  PreviewMouseLeftButtonUp="PreviewScrollViewer_PreviewMouseLeftButtonUp"
                                  PreviewMouseMove="PreviewScrollViewer_PreviewMouseMove">
                        <Grid x:Name="PreviewContent"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center">
                            <Image Source="{Binding PreviewImage}"
                                   Visibility="{Binding HasPreviewImage, Converter={StaticResource BooleanToVisibilityConverter}}"
                                   Stretch="Uniform"
                                   MaxWidth="{Binding ElementName=PreviewScrollViewer, Path=ViewportWidth}"
                                   MaxHeight="{Binding ElementName=PreviewScrollViewer, Path=ViewportHeight}"
                                   RenderOptions.BitmapScalingMode="HighQuality">
                                <Image.LayoutTransform>
                                    <ScaleTransform ScaleX="{Binding ZoomFactor}"
                                                    ScaleY="{Binding ZoomFactor}" />
                                </Image.LayoutTransform>
                            </Image>
                            <TextBlock Text="将包含照片的文件夹拖入窗口"
                                       Foreground="{StaticResource MutedTextBrush}"
                                       FontSize="20"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       Visibility="{Binding HasPreviewImage, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />
                        </Grid>
                    </ScrollViewer>
                </Border>
            </Grid>
        </Border>

        <Border Grid.Column="2"
                AllowDrop="True"
                Margin="0,16,16,16"
                Background="{StaticResource PanelBrush}"
                BorderBrush="{StaticResource PanelBorderBrush}"
                BorderThickness="1"
                CornerRadius="18"
                Padding="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <StackPanel>
                    <TextBlock Text="当前路径"
                               FontSize="18"
                               FontWeight="SemiBold"
                               Margin="0,0,0,8" />
                    <TextBlock Text="{Binding CurrentFolderPath}"
                               TextWrapping="Wrap"
                               Foreground="{StaticResource MutedTextBrush}"
                               Margin="0,0,0,12" />
                    <TextBlock Text="选中照片"
                               FontSize="18"
                               FontWeight="SemiBold"
                               Margin="0,0,0,8" />
                    <TextBlock Text="{Binding SelectedPhotoPath}"
                               TextWrapping="Wrap"
                               Foreground="{StaticResource MutedTextBrush}"
                               Margin="0,0,0,12" />
                </StackPanel>

                <Border Grid.Row="2"
                        Background="#0B1220"
                        BorderBrush="{StaticResource PanelBorderBrush}"
                        BorderThickness="1"
                        CornerRadius="14"
                        Padding="10">
                    <ListBox ItemsSource="{Binding PhotoItems}"
                             SelectedItem="{Binding SelectedPhoto}"
                             BorderThickness="0"
                             Background="Transparent">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Padding" Value="0" />
                                <Setter Property="Margin" Value="0" />
                                <Setter Property="Background" Value="Transparent" />
                                <Setter Property="BorderThickness" Value="0" />
                                <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <ContentPresenter />
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border x:Name="PhotoItemBorder"
                                        Padding="10"
                                        Margin="0,0,0,8"
                                        Background="#111827"
                                        CornerRadius="10"
                                        BorderBrush="#273449"
                                        BorderThickness="1">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="4" />
                                            <ColumnDefinition Width="18" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Border x:Name="SelectedBar"
                                                Width="4"
                                                CornerRadius="2"
                                                Background="#38BDF8"
                                                Opacity="0" />
                                        <Ellipse Grid.Column="1"
                                                 Width="10"
                                                 Height="10"
                                                 Fill="#EF4444"
                                                 HorizontalAlignment="Center"
                                                 VerticalAlignment="Center"
                                                 Visibility="{Binding HasProblem, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                 ToolTip="当前照片有问题" />
                                        <TextBlock Grid.Column="2"
                                                   Text="{Binding FileName}"
                                                   FontWeight="SemiBold"
                                                   Foreground="White"
                                                   Margin="10,0,0,0" />
                                    </Grid>
                                </Border>
                                <DataTemplate.Triggers>
                                    <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Value="True">
                                        <Setter TargetName="PhotoItemBorder" Property="Background" Value="#EFF6FF" />
                                        <Setter TargetName="PhotoItemBorder" Property="BorderBrush" Value="#93C5FD" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Value="True">
                                        <Setter TargetName="PhotoItemBorder" Property="Background" Value="#DBEAFE" />
                                        <Setter TargetName="PhotoItemBorder" Property="BorderBrush" Value="#2563EB" />
                                        <Setter TargetName="SelectedBar" Property="Opacity" Value="1" />
                                    </DataTrigger>
                                </DataTemplate.Triggers>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>







